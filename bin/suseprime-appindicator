#!/usr/bin/python3

import sys
from os.path import abspath, dirname, join

try:
    import suseprimeindicator
except ImportError:
    sys.path.append(abspath(join(dirname(__file__), '..')))

from suseprimeindicator import indicator
indicator.main()
